meta:
  storage:
    type: fs
    path: /var/artipie/repo
  metrics:
    type: log
    interval: 60
  credentials:
    - type: artipie
      storage:
        type: fs
        path: /var/artipie/security
