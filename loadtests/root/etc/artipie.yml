meta:
  storage:
    type: fs
    path: /var/artipie/repo
  layout: org
  metrics:
    type: log
    interval: 60
  credentials:
    - type: artipie
      storage:
        type: fs
        path: /var/artipie/security
